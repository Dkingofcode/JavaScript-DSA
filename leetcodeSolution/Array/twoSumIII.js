class twoSum {
   super(){
     add();
     find();
   }
 let hashMap = {};

   function add(input) {
     let count = hashMap.keyExists(input).length();
     hashMap[input] = count + 1;
   }

   function find(value) {
       for(let pair : pairs){
         
       }
   }
}